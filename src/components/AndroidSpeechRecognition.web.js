// Stub component for web where native Android speech recognition is unavailable
import React from 'react';

const AndroidSpeechRecognition = () => {
  return (
    <div style={{padding: 20}}>
      <p>Android speech recognition is not supported on the web.</p>
    </div>
  );
};

export default AndroidSpeechRecognition;
